Description: Support PostgreSQL 14 through 19 logical decoding APIs
Author: Ruohang Feng <rh@vonng.com>
Forwarded: not-yet

diff -rN -U 0 a/decoder_raw.c b/decoder_raw.c
--- a/decoder_raw.c	2026-07-28 10:43:45
+++ b/decoder_raw.c	2026-08-31 18:57:44
@@ -34,0 +35,9 @@
+#if PG_VERSION_NUM < 170000
+#define DECODER_RAW_TUPLE(change, member) \
+	((change)->data.tp.member == NULL ? NULL : \
+	 &(change)->data.tp.member->tuple)
+#else
+#define DECODER_RAW_TUPLE(change, member) \
+	((change)->data.tp.member)
+#endif
+
@@ -75,0 +85 @@
+#if PG_VERSION_NUM >= 190000
@@ -76,0 +87,3 @@
+#else
+	AssertVariableIsOfType(&_PG_output_plugin_init, LogicalOutputPluginInit);
+#endif
@@ -603 +616 @@
-								   change->data.tp.newtuple);
+								   DECODER_RAW_TUPLE(change, newtuple));
@@ -610,2 +623,4 @@
-				HeapTuple	oldtuple = change->data.tp.oldtuple;
-				HeapTuple	newtuple = change->data.tp.newtuple;
+				HeapTuple	oldtuple =
+					DECODER_RAW_TUPLE(change, oldtuple);
+				HeapTuple	newtuple =
+					DECODER_RAW_TUPLE(change, newtuple);
@@ -627 +642 @@
-								   change->data.tp.oldtuple);
+								   DECODER_RAW_TUPLE(change, oldtuple));
