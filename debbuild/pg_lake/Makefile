#
# pg_lake is currently a placeholder extension: keep packaging metadata in-tree,
# but intentionally skip building/publishing artifacts for now.
#

.PHONY: all setup build move clean

all setup build move clean:
	@echo "pg_lake is a placeholder and is currently disabled; skipping build."

