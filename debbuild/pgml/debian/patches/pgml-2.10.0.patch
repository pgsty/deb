--- a/.cargo/config.toml
+++ b/.cargo/config.toml
@@ -2,10 +2,10 @@
 rustflags = ["-C", "link-args=-Wl,-undefined,dynamic_lookup"]
 
 [target.x86_64-unknown-linux-gnu]
-rustflags = ["-C", "link-args=-Wl,-undefined,dynamic_lookup,-fuse-ld=lld"]
+rustflags = ["-C", "link-args=-Wl,-undefined,dynamic_lookup,-fuse-ld=lld", "-C", "link-arg=-Wl,--build-id=sha1"]
 
 [target.aarch64-unknown-linux-gnu]
-rustflags = ["-C", "link-args=-Wl,-undefined,dynamic_lookup,-fuse-ld=lld"]
+rustflags = ["-C", "link-args=-Wl,-undefined,dynamic_lookup,-fuse-ld=lld", "-C", "link-arg=-Wl,--build-id=sha1"]
 
 [target.aarch64-apple-darwin]
 rustflags = ["-C", "link-args=-WL,-undefined,dynamic_lookup"]
--- a/src/orm/model.rs
+++ b/src/orm/model.rs
@@ -360,7 +360,7 @@
                 )
                 .unwrap()
                 .unwrap();
-                let hyperparams = result.get(11).unwrap().unwrap();
+                let hyperparams = result.get(6).unwrap().unwrap();
 
                 let bindings: Box<dyn Bindings> = match runtime {
                     Runtime::openai => {
--- a/src/orm/project.rs
+++ b/src/orm/project.rs
@@ -142,7 +142,7 @@
                     warning!("Active projects have exceeded capacity map, clearing caches.");
                     projects.clear();
                 }
-                projects.insert(project_id, model_id).unwrap();
+                projects.insert(project_id, model_id);
                 project_id
             }
         };
