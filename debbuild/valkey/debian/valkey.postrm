#!/bin/sh

set -eu

#DEBHELPER#

if [ "$1" = purge ]; then
    for conffile in /etc/valkey/valkey.conf /etc/valkey/sentinel.conf; do
        dpkg-statoverride --remove "$conffile" 2>/dev/null || test $? -eq 2
    done
fi

exit 0
